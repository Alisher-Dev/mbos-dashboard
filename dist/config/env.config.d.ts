export declare const envConfig: {
    port: string;
    database: {
        name: string;
        user: string;
        host: string;
        password: string;
        port: number;
    };
    jwt: {
        accessSecret: string;
        refreshSecret: string;
    };
};
